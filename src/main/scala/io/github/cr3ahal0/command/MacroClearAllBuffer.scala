package command
import data.Buffer
import data.Selection

class MacroClearAllBuffer(buffer : Buffer) extends Command{

  
  override def execute
  {
    //Selectionne tout le buffer
    
    //Delete le buffer avec la commande erase
    
  }
  
  
}